_site
.sass-cache
.jekyll-cache
.jekyll-metadata
vendor
.bundle
.vendor
.news_state.json
# Per-day Big Picture summary cache (see ROADMAP Phase 4). One file per
# PT-day; the daily edition cron overwrites the same file 3x/day, so they
# are runtime artifacts, not source.
*-bp.json
Gemfile
Gemfile.lock
__pycache__/
*.pyc
*.pyo
generate_news.log*
# Per-feed health tracking for the RSS health monitor (tools/check_feeds.py).
# Updated every cron run; not version-controlled.
.feed_health.json
# Per-edition OG social card images (tools/make_og_image.py). Generated
# on each edition run; ~200KB each, regenerated daily so no need to track.
assets/og/
