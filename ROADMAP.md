# AI News Development Roadmap

This file tracks the autonomous evolution of the AI News site.

## 🎯 Strategic Goals
- Transform from a basic digest to a high-signal, visually stunning AI intelligence hub.
- Automate the a-z pipeline from discovery to publication.
- Maintain 100% test coverage for core logic.

## 🗺️ Roadmap

### Phase 1: The "Sleek" Facelift (UI/UX)
- [x] **Custom CSS:** Implement a modern "Dark Mode" aesthetic (Inter font, subtle borders, high contrast). *Reverted per Issue #2 (user preference: Minima defaults). assets/main.scss removed 2026-06-01.*
- [x] **Mobile Optimization:** Refine the layout for mobile readers.
- [x] **Better Visual Hierarchy:** Improve the "Summary" vs "Details" contrast.

### Phase 2: Intelligence Upgrade (LLM/Content)
- [x] **Prompt Iteration:** Move from "summarization" to "insight synthesis" in `summary_prompt.txt`.
- [x] **Global Executive Summary:** Add a top-level "The Big Picture" section for each edition.
- [ ] **Signal Expansion:** Identify and integrate 5+ new high-signal RSS/X sources. *(In progress: Phase 4 #1)*

### Phase 3: Architectural Robustness
- [ ] **Automatic Tagging:** Implement keyword extraction to tag posts (e.g., #LLM, #Agentic).
- [ ] **Advanced Deduplication:** Move beyond Jaccard to embedding-based similarity if needed.

### Phase 4: Reliability & Content (Active — 2026-06-05+)
The current critical path. Items here are addressed by the `ai-news-roadmap-daily` cron (one per day) and discovered by `ai-news-brainstorm-daily`.

- [x] **Nitter fallback chain** — implemented as `_http_get_with_retry` + per-feed fallback chain in `fetch_feed()`. 26 nitter feeds wired with privacydev.net/privacyredirect.com/xcancel.com alternates. *Completed 2026-06-05 (commit feb67b9).*
- [x] **`.news_state.json` no longer tracked in git** — untracked in commit feb67b9. *Completed 2026-06-05.*
- [x] **Per-feed isolation + retry/backoff in `fetch_feed()`** — `_http_get_with_retry` (3 attempts, exponential backoff, RSS body-shape validation) wraps the urllib call; `fetch_feed()` swallows per-feed errors and never aborts the edition. *Completed 2026-06-05 (commit feb67b9).*
- [x] **Parallelize LLM section summaries** — `_query_ollama()` extracted as reusable single-call helper; new `summarize_sections_concurrent()` dispatches section summaries via `concurrent.futures.ThreadPoolExecutor` (capped at `MAX_SUMMARY_WORKERS=6`). `generate_post()` loop refactored to build a `section_jobs` list, dispatch in parallel, and preserve render order. 12 new tests in `tests/test_parallel_summaries.py` confirm parallel execution and correctness. *Completed 2026-06-05 (commit pending).*
- [x] **Reuse "Big Picture" across same-day editions** — `generate_post()` now checks for a per-day `<pt-date>-bp.json` cache (keyed on the `(source,title)` fingerprint of the day's article set) and reuses the cached `summary_text` + pre-rendered `summary_html` for subsequent same-day editions. Cache file is gitignored (`*-bp.json`). Morning edition is canonical; Afternoon/Evening reuse. 18 new tests in `tests/test_big_picture_cache.py`. *Completed 2026-06-05 (commit 2663d30).*
- [x] **Cross-edition dedup tightens from exact-link to last-24h-Jaccard** — `is_duplicate()` now also scans `seen_links` entries with a `seen_at` timestamp inside the last `CROSS_EDITION_DEDUP_HOURS=24` and flags a different-link, similar-title story as a duplicate. New entries stamped `seen_at` at write time; `load_state()` migrates legacy entries (no `seen_at`) to "now" so they fall outside the window and old behavior is preserved. 23 new tests in `tests/test_cross_edition_dedup.py`. *Completed 2026-06-06 (commit 3760fba).*
- [x] **Move `test_clean.py` to `tests/` + add `__main__` guard** — root-level `test_clean.py` renamed to `tests/clean_title.py`; demo loop preserved inside an `if __name__ == "__main__":` guard so `python3 tests/test_logic.py` no longer pollutes stdout. New `tests/test_clean_title.py` adds 30 unit tests (whitespace, every escape char, demo-input regression, and an import-silence test). Existing `test_logic.py` updated to import from the new path. All 124 tests across 10 test files pass. *Completed 2026-06-08 (commit b437836).*
- [x] **Add RSS feed health monitor** — `tools/check_feeds.py` runs from a separate cron, alerts to Discord when a feed has been failing for 3+ consecutive runs. Tracks per-feed consecutive failures in `.feed_health.json`; supports `--dry-run` and `--json`; Discord alert via `DISCORD_WEBHOOK_URL` env var. 27 tests in `tests/test_feed_health.py`. *Completed 2026-06-09 (commit 893b9df).*
- [x] **Add 3 new sources: arXiv cs.AI, HuggingFace Trending Models, The Rundown AI** — added arXiv cs.AI under new Research section, HuggingFace Trending Models (blog feed) under Developer Tools > HuggingFace, The Rundown AI (via Google News RSS proxy) under News > Newsletters. 14 new tests in `tests/test_new_feeds.py`. *Completed 2026-06-10 (commit da21f1d).*
- [x] **Fill in `index.markdown`** — home page now shows recent editions (last 9), section index, and archive link. *Completed 2026-06-09.*
- [x] **Add site-level RSS feed (`feed.xml`)** — `jekyll-feed` was already generating feed.xml but with relative URLs (empty `url` in `_config.yml`). Fixed: set `url` and `baseurl` for absolute URLs, added `feed.posts_limit: 50` (was default 10), added `robots.txt` referencing both sitemap.xml and feed.xml. 26 tests in `tests/test_feed.py`. *Completed 2026-06-11 (commit d3c14b6).*

### Phase 5: Discovery (Brainstorm Backlog)
Items here were brainstormed by the `ai-news-brainstorm-daily` cron. Each is a candidate for Phase 4 if the daily cron picks it.

- [x] **Add a per-day "All editions" landing page (`/news/:year/:month/:day/`)** — Jekyll generator plugin (`_plugins/day_index_generator.rb`) creates an `index.html` for every day with edition posts, grouping Morning/Afternoon/Evening editions with Big Picture teasers extracted from each post. Custom `_layouts/day.html` layout renders the day page. 22 tests in `tests/test_day_index.py`. *Completed 2026-06-12 (commit 04dff7b).*
- [x] **Add JSON-LD `NewsArticle` schema to generated posts** — `_posts/*.html` has no structured data; Jekyll's SEO plugin only adds WebSite-level schema on the home page. Embed a `<script type="application/ld+json">` block (datePublished, headline from "Big Picture", articleSection, keywords) in `generate_news.py`'s render path so search engines can index individual editions as news articles. 16 new tests in `tests/test_json_ld.py`. *Completed 2026-06-13 (commit 6d3f14d).*
- [x] **Convert source-name labels (`<strong>FT AI</strong>` etc.) into linked source pills** — each item currently renders the source name as a plain bold string. In `generate_news.py`'s post-render step, replace with `<a class="source-pill" href="<source_homepage>">` where `source_homepage` is looked up from a new `source_urls` map in `sections.json`. Improves navigation back to original publishers and gives visual consistency. *Completed 2026-06-14 (commit 885a443).*
- [x] **Add `/archive/` page grouped by month with per-month counts** — Jekyll generator plugin (`_plugins/archive_index_generator.rb`) creates `/archive/index.html` grouping all edition posts by YYYY-MM in reverse chronological order with per-month edition counts. Custom `_layouts/archive.html` layout with semantic CSS classes. 20 tests in `tests/test_archive.py`. *Completed 2026-06-15 (commit 3ef9f28).*
- [x] **Expose `TIMEZONE` in `config.json` and read it from both `run_edition.sh` and `pacific_now()`** — `America/Los_Angeles` is hardcoded in two places (`run_edition.sh` `TZ=`, `generate_news.py` `pacific_now()`). Centralize into `config.json` so future contributors in other zones (or DST edge cases) can override without code edits. *Completed 2026-06-16 (commit 08ce2f3).*
- [x] **Parallelize RSS feed fetching alongside the existing LLM parallelization** — `fetch_all_feeds()` dispatches all feed fetches via `ThreadPoolExecutor(max_workers=MAX_FEED_WORKERS=10)`, preserving canonical section/subsection/feed order for dedup. 5 new tests in `tests/test_parallel_feeds.py`. *Completed 2026-06-09.*
- [x] **Filter "R to @user:" retweet-only items out of rendered posts** — `is_retweet()` filters items with titles starting with `R to @` in `fetch_feed()` (primary) and `render_item()` (safety net). 10 new tests in `tests/test_retweet_filter.py`. *Completed 2026-06-09.*
- [x] **Group home page (`index.markdown`) by date with per-day edition counts instead of a flat 5-deep list** — custom `_layouts/home.html` groups posts by date with Morning/Afternoon/Evening edition badges, day headings linking to day index pages, and CSS styling. Updated `index.markdown` with Liquid grouping logic and edition badge detection. 26 new tests in `tests/test_home_page.py`. Updated `test_feed.py` for RSS type and subscribe link. *Completed 2026-06-17 (commit ec991d1).*
- [x] **Add `jekyll-sitemap` plugin and ship a `sitemap.xml`** — added `jekyll-sitemap` to `Gemfile` and `_config.yml`; `sitemap.xml` now generated on build. *Completed 2026-06-09.*
- [x] **Add log rotation to `generate_news.log`** — `RotatingFileHandler` (10MB × 3 backups) replaces the plain `FileHandler`. `generate_news.log*` added to `.gitignore`. *Completed 2026-06-09.*
- [x] **Move tunable constants (`MAX_ITEMS_PER_SOURCE`, `MAX_AGE_DAYS`, `TITLE_SIM_THRESHOLD`, `CROSS_EDITION_DEDUP_HOURS`) from module-level globals into `config.json`** — `config.json` now has a `tuning` section; `get_tuning(site_root)` merges defaults with user overrides. `is_duplicate()` and `fetch_feed()` accept these as keyword args. Module-level constants stay as fallback defaults. 9 new tests in `tests/test_tuning_config.py`. *Completed 2026-06-09.*
- [ ] **Generate a daily social card (OG image) per edition using the "Big Picture" first sentence** — the Jekyll SEO tag already injects WebSite-level `og:title` / `og:description` on the home page, but individual `_posts/*.html` posts have no `og:image` and no `twitter:image` — link previews on X/LinkedIn/Slack show a blank box. Add a `tools/make_og_image.py` (Pillow, no extra dep on most systems) that renders a 1200×630 PNG with the edition title + Big Picture excerpt, called from `run_edition.sh` after `generate_news.py` writes the post, and front-mattered as `image: /ai-news/assets/og/YYYY-MM-DD-<Edition>.png`. New items gitignored; site builds them on demand. 2026-06-07.
- [ ] **Surface feed-level "item age" and "is stale" warnings in the edition's `Scanning N feeds` line** — current front matter is `Scanning 31 feeds · 2 accounts posted · 2 items`, which gives no signal about whether the items are from today or 6 days old. Extend the header line to include a freshness tally (e.g. `· 2 fresh, 0 stale, 1 from yesterday`) computed from `MAX_AGE_DAYS` vs. item `published` timestamp. Catches silent feed-staleness visually without needing `tools/check_feeds.py` cron to alert. 2026-06-07.
- [ ] **Add a per-edition "Reading time" estimate + per-section item count badges to the post header** — readers currently have no sense of how long an edition will take. Compute `reading_time = ceil(total_word_count / 230)` minutes from the rendered `summary_text` + item descriptions during `generate_post()`, and inject it into the `Scanning N feeds` line as `· ~4 min read`. Also add a small per-section header badge like `News (3)` so the longest sections (AI Labs, News) are obvious at a glance. 2026-06-08.
- [ ] **Expose the day's edition data as a static JSON endpoint at `/api/2026-06-08.json`** — `generate_news.py` already builds a structured `section_jobs` list with title/summary/items/links during render; emit a sibling `<pt-date>-<Edition>.json` next to each `.html` post (gitignored, like `*-bp.json`) and add a Jekyll `static_file` route so it serves at `/api/:year-:month-:day-<Edition>.json`. Lets power users / aggregators consume the digest without scraping HTML, and gives the LLM-summary output a stable machine-readable home. 2026-06-08.
- [ ] **Add a `/week/` page that shows the last 7 days' "Big Picture" summaries as a timeline** — the most valuable editorial content (the LLM-synthesized Big Picture) is currently locked inside individual edition posts and impossible to scan across days. Add a `week.html` page (Jekyll layout or static generator) that walks the last 7 days of posts, extracts each `🌍 The Big Picture` paragraph, and renders them as a reverse-chronological timeline with edition labels. Surfaces the editorial voice of the digest and gives returning readers a one-stop "what happened this week" view. 2026-06-08.
- [x] **Cap `.news_state.json` growth with a TTL-based pruning pass at load time** — `prune_state()` removes entries older than `MAX_HISTORY_DAYS=60` (conservative for entries without `seen_at`). Called inside `load_state()` after migration. 10 new tests in `tests/test_state_pruning.py`. *Completed 2026-06-09.*
- [ ] **Add `robots.txt` pointing to sitemap.xml** — the site generates `sitemap.xml` via `jekyll-sitemap` but has no `robots.txt` to direct crawlers to it. Add a static `robots.txt` (allow all, reference sitemap) in the Jekyll root for an easy SEO win. 2026-06-09.
- [ ] **Inject Open Graph `article` meta tags into generated posts** — individual `_posts/*.html` pages lack `og:type=article` and `article:published_time` tags; `jekyll-seo-tag` only generates `WebSite`-level schema on the home page. Modify `generate_news.py`'s render to emit `<meta property="og:type" content="article">`, `og:published_time`, and `og:description` (from Big Picture excerpt) into each post's `<head>` so link previews on X/LinkedIn/Slack show rich cards per edition. 2026-06-09.
- [ ] **Add retry/backoff for `jekyll build` in `run_edition.sh`** — the build step (`bundle exec jekyll build`) has no retry; a transient Bundler or network issue kills the entire edition. Wrap it in a 2-attempt retry loop with 30s backoff so intermittent failures don't cause a missed edition. 2026-06-09.
- [ ] **Extract inline styles in generated posts into a shared CSS class** — `generate_news.py` renders inline styles (e.g., `<div style="background: #f9f9f9; padding: 15px; border-left: 5px solid #ccc; ...">`) on every Big Picture block and other elements. Move these to a CSS class in `assets/main.scss` and reference the class instead, reducing post size ~200 bytes each and enabling site-wide style tweaks without regenerating old posts. 2026-06-09.
- [x] **Add TTS audio generation for section and Big Picture summaries** — `generate_audio()` uses edge-tts (Microsoft Edge TTS) to produce MP3 files per section and Big Picture summary, stored in `assets/audio/<edition>/`. Posts embed `<audio>` players inline next to each summary. Configurable via `config.json` `tts` section (voice, rate, enabled). Graceful degradation: if edge-tts is missing or generation fails, the post renders without audio players. 26 tests in `tests/test_tts.py`. *Completed 2026-06-10 (commit 2709b19).*
- [ ] **Add a client-side search page with Lunr.js** — the site has 90+ posts and no way to find a past story. Add a `/search/` page that builds a Lunr.js index from all `site.posts` at Jekyll build time and provides a search box. Enables readers to locate specific topics across all editions without server-side infrastructure. 2026-06-09.
- [ ] **Add Discord alert on `run_edition.sh` failure** — `check_feeds.py` already posts to Discord on feed-health issues, but `run_edition.sh` silently aborts with `set -e` on Jekyll build failure or Python errors. Add a `trap ... ERR` handler that calls a small `tools/alert.sh` (curl to `$DISCORD_WEBHOOK_URL`) with the edition name, date, and the failing command, so missed editions are caught immediately. 2026-06-13.
- [ ] **Add `audio/` to `.gitignore` and ship audio via a separate deploy step** — MP3 files for TTS are committed directly to the repo (`assets/audio/`), which will grow the git history unboundedly (~1 MB per edition × 3 editions/day). Add `assets/audio/` to `.gitignore`, move audio uploads to a separate `tools/deploy_audio.sh` that pushes to GitHub Releases or an S3 bucket, and update `<audio>` src attributes to the external URL. Keeps the repo lean and clone fast. 2026-06-13.
- [ ] **Add `tags:` front-matter with auto-extracted topic keywords per post** — `generate_news.py` renders posts with `categories: news digest` but no topic tags. Extract the top 3-5 noun phrases from each section summary (simple regex over capitalized terms, no LLM call needed) and inject them as `tags: [LLM, IPO, open-weights]` in the YAML front matter. Jekyll then generates `/tag/llm/` pages automatically, giving readers topic-level navigation. 2026-06-13.
- [ ] **Generate a `/now/` page showing today's editions only** — the home page lists the last 9 editions across multiple days, but returning readers want "what's new today" at a glance. Add a Jekyll page at `/now/` that filters `site.posts` to `site.time | date: '%Y-%m-%d'` and shows only today's Morning/Afternoon/Evening editions with their Big Picture excerpts, giving a focused "current briefing" experience. 2026-06-13.
- [ ] **Add previous/next edition navigation links to each post** — each edition post is currently isolated with no sequential navigation. Add "← Morning | Afternoon →" links at the bottom of each post using Jekyll's `page.previous`/`page.next` or date+edition ordering logic in `_layouts/post.html`, improving reader flow between consecutive editions. 2026-06-17.
- [ ] **Add `--dry-run` flag to `run_edition.sh`** — the script always commits and pushes after building; a `--dry-run` flag would run `generate_news.py` and `jekyll build` but skip the `git add/commit/push` steps, enabling safe testing of pipeline changes without publishing a broken edition. 2026-06-17.
- [ ] **Add `lastmod` timestamps to `sitemap.xml` entries** — `jekyll-sitemap` generates URLs without `<lastmod>` dates, but Google's news crawler prefers fresh timestamps for crawl prioritization. Override the default sitemap layout or add a Jekyll plugin that injects `lastmod` from each post's `date` front matter. Easy SEO win for a news site. 2026-06-17.
- [ ] **Add `aria-label` accessibility attributes to audio players and edition badges** — generated posts embed `<audio>` players and `.edition-badge` spans without ARIA attributes; screen readers can't identify them. Add `aria-label="Audio summary of News section"` to each `<audio>` and `aria-label="Morning Edition"` to each badge in `generate_news.py`'s render path. 2026-06-17.
- [ ] **Add a `/stats/` page showing feed health and contribution counts** — surface `.feed_health.json` data as a public Jekyll page showing which feeds are active, their last-seen timestamps, and how many items each contributed recently. Gives readers transparency about coverage and helps identify dead sources. 2026-06-17.

---

## 🛠️ Operating Procedures
1. **Issue Sync:** Check GitHub Issues for user feedback → Convert to tasks here.
2. **Dev Cycle:** Pick task → Implement → Run `tests/test_logic.py` → Push.
3. **Communication:** Close GitHub Issues with a summary of the fix/improvement.

## 🤖 Autonomous Maintenance (NEW — 2026-06-05)
Two new cron jobs run daily on this repo's behalf. Both MUST read this file before acting, and both MUST update this file when they change something.

- **`ai-news-roadmap-daily`** (every day) — reads ROADMAP.md, picks exactly ONE item from Phase 4 (highest-priority unchecked item), implements it (per Operating Procedure #2: write tests, run them, commit, push), and marks it `[x]`. Then reports 1-2 lines to Discord via cron auto-delivery. If Phase 4 is empty, picks the top item from Phase 5 instead. SKIPS if the working tree is dirty.
- **`ai-news-brainstorm-daily`** (every day) — reads the current `~/ai-news/`, the live site structure, and this roadmap, brainstorms 3-5 new concrete improvement ideas, and appends them to Phase 5 with today's date. Does NOT implement — only discovers. If Phase 5 has more than 15 unchecked items, SKIPS and reports (backlog is full).

These crons are intentionally separated so that discovery (brainstorm) and execution (roadmap) cannot step on each other.
